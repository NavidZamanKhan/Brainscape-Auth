/// Spacing, radius, and dimension constants used across the app.
///
/// Eliminates magic numbers and ensures visual consistency.
class AppDimensions {
  AppDimensions._();

  // ── Border radius ──
  static const double radiusSm = 8.0;
  static const double radiusMd = 16.0;
  static const double radiusLg = 20.0;
  static const double radiusXl = 24.0;

  // ── Padding / spacing ──
  static const double spacingXs = 4.0;
  static const double spacingSm = 8.0;
  static const double spacingMd = 12.0;
  static const double spacingLg = 16.0;
  static const double spacingXl = 20.0;
  static const double spacingXxl = 24.0;
  static const double spacing3xl = 32.0;
  static const double spacing4xl = 36.0;
  static const double spacing5xl = 40.0;

  // ── Progress bar ──
  static const double progressBarHeight = 8.0;

  // ── Option tile ──
  static const double optionLabelSize = 36.0;
  static const double optionBorderWidth = 1.5;

  // ── Score circle ──
  static const double scoreCircleSize = 180.0;
  static const double scoreArcStrokeWidth = 12.0;

  // ── Button ──
  static const double buttonVerticalPadding = 18.0;

  // ── Welcome screen hero icon ──
  static const double heroIconContainerSize = 100.0;
  static const double heroIconSize = 52.0;

  // ── Glass card ──
  static const double glassCardBorderWidth = 1.0;
  static const double glassCardBlurRadius = 24.0;
  static const double glassCardShadowOffset = 8.0;
}
